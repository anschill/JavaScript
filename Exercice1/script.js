function myFunction
{
  var dividende = document.GetElementbyID(firstNumber).value;
  var diviseur = document.GetElementbyID(secondNumber).value;

  var quotient = (dividende % diviseur);

  alert(`Resultat = ${quotient}`);

}
//DOM pour récupérer le Diviseur et le Dividende

//Récupérer la partie entière du quotient

//
